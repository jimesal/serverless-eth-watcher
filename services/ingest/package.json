{
  "name": "ingest-lambda",
  "private": true,
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/chai": "^4.3.4",
    "@types/jest": "^29.5.14",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.0.9",
    "aws-sdk-client-mock": "^4.1.0",
    "chai": "^4.3.7",
    "esbuild": "^0.27.2",
    "jest": "^29.7.0",
    "mocha": "^10.2.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "jest --config ./jest.config.cjs",
    "test:watch": "jest --config ./jest.config.cjs --watch",
    "build": "esbuild src/handler.ts --bundle --platform=node --outfile=dist/handler.js --sourcemap",
    "debug": "npm run build && node --inspect-brk dist/handler.js"
  }
}
