{
  "name": "webhook-manager",
  "private": true,
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^25.0.9",
    "esbuild": "^0.27.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --outfile=dist/handler.js --sourcemap",
    "test": "jest --config ./jest.config.cjs"
  }
}
